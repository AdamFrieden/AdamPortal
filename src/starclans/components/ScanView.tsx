import { Box, Typography } from '@mui/material';

const ScanView = () => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4">Scan Network</Typography>
      <Typography>Coming soon: Scan the network for opportunities and threats.</Typography>
    </Box>
  );
};

export default ScanView; 