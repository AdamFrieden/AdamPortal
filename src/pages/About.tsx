// pages/About.tsx
import { Typography } from '@mui/material';

const About = () => {
  return (
    <div>
      <Typography variant="h4">About Us</Typography>
      <Typography>
        This is the about page for Adam Portal.
      </Typography>
    </div>
  );
};

export default About;
